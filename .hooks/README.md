To use the hooks run

    git config --local core.hooksPath .hooks

from your shell.

You can also symlink the hooks into the `.git/hooks/` directory. To
ignore the hooks when committing, use `git commit -n`
